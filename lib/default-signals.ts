/** Minimal valid signals for local Geval runs (adjust metrics to match your rules). */
export const DEFAULT_SIGNALS_JSON = `{
  "name": "demo-signals",
  "version": "1.0.0",
  "signals": [
    { "metric": "accuracy", "value": 0.92 }
  ]
}
`;
